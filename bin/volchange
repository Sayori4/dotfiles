#!/bin/bash

if [[ $1 == "inc" ]]; then
	wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
	VOL=$(wpctl get-volume @DEFAULT_AUDIO_SINK@)
	notify-send "Volume Control" "$VOL"
elif [[ $1 == "dec" ]]; then
	wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
	VOL=$(wpctl get-volume @DEFAULT_AUDIO_SINK@)
	notify-send "Volume Control" "$VOL"
elif [[ $1 == "mute" ]]; then
	wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
	VOL=$(wpctl get-volume @DEFAULT_AUDIO_SINK@)
	notify-send "Volume Control" "$VOL"
fi